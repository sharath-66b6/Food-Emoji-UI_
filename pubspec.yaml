name: food_emoji_ui
description: A Flutter app for displaying food emojis with categories, about, and contact sections.
publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Add any additional dependencies here if needed, e.g., icons, animations, etc.

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # Specify any assets (e.g., logos, images) here if you have any
  # Example asset; update or remove if not needed
